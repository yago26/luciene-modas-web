"use client";

import { useState } from "react";

export default () => {
  const criar = async () => {
    await fetch("");
  };

  const atualizar = async () => {
    await fetch("");
  };

  const excluir = async () => {
    await fetch("");
  };

  const [form, setForm] = useState({
    conteudo: "",
    avaliacao_produto: 0,
  });

  return <div>{}</div>;
};
